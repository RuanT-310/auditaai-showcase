// --- Interfaces de Dados ---
interface Award {
  title: string;
  event: string;
  color: string;
}

interface Feature {
  title: string;
  description: string;
  iconPath: string;
  iconColor: string;
}

interface Step {
  title: string;
  description: string;
}

interface Partner {
  name: string;
  description: string;
}
